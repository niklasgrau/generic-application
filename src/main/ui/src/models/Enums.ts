export enum OfferStatusET {
    CREATED = "CREATED",
    WAITING_FOR_APPROVAL = "WAITING_FOR_APPROVAL",
    SENT = "SENT",
    DECLINED = "DECLINED",
    EXPIRED = "EXPIRED",
}

export enum ProjectTypeET {
    FIXED_PRICE,
    TIME_AND_MATERIAL,
    SERVICE,
}

export enum ContactTypeET {
    JURISTIC_PERSON = "JuristicPerson",
    NATURAL_PERSON = "NaturalPerson"
}